#include <cstdio>
#include <vector>
using namespace std;

vector<int> v;
void recur(int s, int e)
{
}
int main()
{
    int num;
    FILE *in;
    while (scanf("%d", &num) != -1)
    {
        v.push_back(num);
    }

    return 0;
}